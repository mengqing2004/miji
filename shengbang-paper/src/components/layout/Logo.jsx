import React from 'react';

function Logo() {
    return (
        <div className={`w-full bg-red-50 h-[70px] `}>
            {/*logo*/}
            <p className={`flex items-center justify-center w-full h-full`}>LOGO</p>
        </div>
    );
}

export default Logo;